# Local .terraform directories
.terraform/

# .tfstate files
*.tfstate
*.tfstate.*

# Crash log files
crash.log

# Exclude all .tfvars files, which are likely to contain sensitive data,
# unless the file is explicitly named 'example.tfvars' or 'vars.tf'
*.tfvars
*.tfvars.json

# Ignore override files as they are meant to be used locally
override.tf
override.tf.json